<div id="currency_detail_chart" symbol="{{data.symbol}}" width="100%" height="400px"></div>
{# Now we attach the Chrts JS library In a Twig template. #}
{{ attach_library('crypto_chart/currency_detail_chart') }}